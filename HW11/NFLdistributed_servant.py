def varprobe(iq,oq):
	loopcoutner = 0
	while True:
		try: 
			task = iq.get()
			if task[0]!=loopcounter:
				read model
				loopcounter+=1
			iterate over queue
			probe variable 
			report results 

		break out when all nodes are terminate

if __name__ == "__main__":
		#connect to queue
		create processes (varprobe)
		
